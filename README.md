# Wave SMP Next.js

Omgezet vanuit wave_smp_alive_dead_filters(1).zip.

Deploy:
1. Upload naar GitHub.
2. Import in Vercel.
3. Vercel gebruikt automatisch `npm run build`.

API placeholders:
- `/api/applications`
- `/api/tebex/packages`
