'use client';
import {useState} from 'react';
export default function Admin(){const[code,setCode]=useState('');const[apps,setApps]=useState<any[]>([]);const[err,setErr]=useState('');async function load(){const r=await fetch('/api/admin/applications',{headers:{'x-admin-code':code}});const j=await r.json();if(!r.ok){setErr(j.error||'Geen toegang');return}setApps(j.applications);setErr('')}async function decide(id:string,status:string){await fetch('/api/admin/decision',{method:'POST',headers:{'Content-Type':'application/json','x-admin-code':code},body:JSON.stringify({id,status})});load()}return <main className="section"><h1 className="title">Admin</h1><div className="adminBox"><label>Speciale admin code</label><input className="input" type="password" value={code} onChange={e=>setCode(e.target.value)}/><button className="btn" onClick={load}>Inloggen</button>{err&&<p>{err}</p>}{apps.map(a=><div className="adminItem" key={a.id}><h3>{a.minecraft_name} <span className="status">{a.status}</span></h3><p><b>Discord:</b> {a.discord_name}</p><p><b>Leeftijd:</b> {a.age}</p><p><b>Motivatie:</b> {a.motivation}</p><p><b>Roleplay:</b> {a.roleplay_experience}</p><button className="btn" onClick={()=>decide(a.id,'accepted')}>Accepteren + whitelist</button> <button className="btn danger" onClick={()=>decide(a.id,'denied')}>Afwijzen</button></div>)}</div></main>}
