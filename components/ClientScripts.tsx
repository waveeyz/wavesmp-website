"use client";

import { useEffect } from "react";

export default function ClientScripts() {
  useEffect(() => {
    if (typeof window === "undefined") return;
    if ((window as any).__waveSmpScriptLoaded) return;
    (window as any).__waveSmpScriptLoaded = true;
    const script = document.createElement("script");
    script.textContent = "\nconst DISCORD_URL = \"https://discord.gg/v6qWFhDBvc\";\nconst ADMIN_CODE = \"Wv$9xQ!2mN#74Lp@Wave\";\n\nfunction getApplications(){\n  return JSON.parse(localStorage.getItem(\"wavesmp_applications\") || \"[]\");\n}\n\nfunction setApplications(apps){\n  localStorage.setItem(\"wavesmp_applications\", JSON.stringify(apps));\n}\n\nfunction escapeHtml(value){\n  return String(value || \"\")\n    .replaceAll(\"&\", \"&amp;\")\n    .replaceAll(\"<\", \"&lt;\")\n    .replaceAll(\">\", \"&gt;\")\n    .replaceAll('\"', \"&quot;\")\n    .replaceAll(\"'\", \"&#039;\");\n}\n\nfunction teamLabel(team){\n  return team && team !== \"none\" ? team : \"Binnenkort\";\n}\n\nfunction mcAvatar(username, size = 96){\n  const clean = encodeURIComponent(String(username || \"Steve\").trim());\n  return `https://minotar.net/avatar/${clean}/${size}`;\n}\n\nfunction getPlayerStats(player){\n  return {\n    kills: player.kills || 0,\n    fish: player.fish || 0,\n    walked: player.walked || 0,\n    jumps: player.jumps || 0,\n    deaths: player.deaths || 0\n  };\n}\n\nfunction statusLabel(status){\n  if(status === \"approved\") return '<span class=\"status-approved\">Goedgekeurd</span>';\n  if(status === \"denied\") return '<span class=\"status-denied\">Afgewezen</span>';\n  return '<span class=\"status-pending\">In behandeling</span>';\n}\n\nfunction setActiveNav(){\n  const file = location.pathname.split(\"/\").pop() || \"index.html\";\n  const map = {\n    \"index.html\":\"home\",\n    \"players.html\":\"players\",\n    \"teams.html\":\"teams\",\n    \"aanmelden.html\":\"aanmelden\",\n    \"shop.html\":\"shop\",\n    \"admin.html\":\"admin\"\n  };\n  const active = map[file] || \"home\";\n  document.querySelectorAll(\".nav\").forEach(a => {\n    a.classList.toggle(\"active\", a.dataset.active === active);\n  });\n}\n\nfunction renderPlayers(){\n  const approved = getApplications().filter(app => app.status === \"approved\");\n\n  const grid = document.getElementById(\"playersGrid\");\n  if(grid){\n    grid.innerHTML = approved.length ? approved.map(player => {\n      const stats = getPlayerStats(player);\n      return `\n        <button class=\"player-card player-card-clickable\" onclick=\"showPlayerStats(${player.id})\">\n          <img class=\"mc-head\" src=\"${mcAvatar(player.username, 96)}\" alt=\"${escapeHtml(player.username)}\">\n          <h3>${escapeHtml(player.username)}</h3>\n          <p><b>Status:</b> ${player.alive === false ? \"<span class=\\\"dead\\\">Dead</span>\" : \"<span class=\\\"alive\\\">Alive</span>\"}</p>\n          <p><b>Kant:</b> ${escapeHtml(teamLabel(player.team))}</p>\n          <div class=\"mini-stats\">\n            <span>\u2694 ${stats.kills}</span>\n            <span>\ud83d\udc1f ${stats.fish}</span>\n            <span>\ud83d\udc5f ${stats.walked}</span>\n          </div>\n        </button>\n      `;\n    }).join(\"\") : `<div class=\"empty-state\">Nog geen geaccepteerde spelers. Accepteer iemand in het admin panel.</div>`;\n  }\n\n  const adminGrid = document.getElementById(\"adminPlayersGrid\");\n  if(adminGrid){\n    adminGrid.innerHTML = approved.length ? approved.map(player => {\n      const stats = getPlayerStats(player);\n      return `\n        <div class=\"player-card\">\n          <img class=\"mc-head\" src=\"${mcAvatar(player.username, 96)}\" alt=\"${escapeHtml(player.username)}\">\n          <h3>${escapeHtml(player.username)}</h3>\n          <p><b>Status:</b> ${player.alive === false ? \"<span class=\\\"dead\\\">Dead</span>\" : \"<span class=\\\"alive\\\">Alive</span>\"}</p>\n\n          <label class=\"team-select-label\">Kant kiezen</label>\n          <select class=\"team-select\" onchange=\"setPlayerTeam(${player.id}, this.value)\">\n            <option value=\"none\" ${!player.team || player.team === \"none\" ? \"selected\" : \"\"}>Geen kant</option>\n            <option value=\"Noord\" ${player.team === \"Noord\" ? \"selected\" : \"\"}>Noord</option>\n            <option value=\"Zuid\" ${player.team === \"Zuid\" ? \"selected\" : \"\"}>Zuid</option>\n            <option value=\"Oost\" ${player.team === \"Oost\" ? \"selected\" : \"\"}>Oost</option>\n            <option value=\"West\" ${player.team === \"West\" ? \"selected\" : \"\"}>West</option>\n          </select>\n\n          <div class=\"stat-editor\">\n            <label>Kills <input type=\"number\" min=\"0\" value=\"${stats.kills}\" onchange=\"updatePlayerStat(${player.id}, 'kills', this.value)\"></label>\n            <label>Vissen <input type=\"number\" min=\"0\" value=\"${stats.fish}\" onchange=\"updatePlayerStat(${player.id}, 'fish', this.value)\"></label>\n            <label>Gelopen <input type=\"number\" min=\"0\" value=\"${stats.walked}\" onchange=\"updatePlayerStat(${player.id}, 'walked', this.value)\"></label>\n            <label>Jumps <input type=\"number\" min=\"0\" value=\"${stats.jumps}\" onchange=\"updatePlayerStat(${player.id}, 'jumps', this.value)\"></label>\n            <label>Deaths <input type=\"number\" min=\"0\" value=\"${stats.deaths}\" onchange=\"updatePlayerStat(${player.id}, 'deaths', this.value)\"></label>\n          </div>\n\n          \n<div class=\"life-toggle-wrap\">\n  <button class=\"life-toggle ${player.alive === false ? \"dead-btn\" : \"alive-btn\"}\"\n    onclick=\"togglePlayerLife(${player.id})\">\n    ${player.alive === false ? \"\ud83d\udc80 Zet op levend\" : \"\u2764\ufe0f Zet op dood\"}\n  </button>\n</div>\n\n<p><b>Whitelist:</b> /whitelist add ${escapeHtml(player.username)}</p>\n\n        </div>\n      `;\n    }).join(\"\") : `<div class=\"empty-state\">Nog geen geaccepteerde spelers.</div>`;\n  }\n\n  renderTeamOverview();\n}\n\nfunction updatePlayerStat(id, field, value){\n  const num = Math.max(0, parseInt(value || \"0\", 10) || 0);\n  const apps = getApplications().map(app => app.id === id ? {...app, [field]: num} : app);\n  setApplications(apps);\n  renderAll();\n}\n\nfunction setPlayerTeam(id, team){\n  const apps = getApplications().map(app => app.id === id ? {...app, team} : app);\n  setApplications(apps);\n  renderAll();\n}\n\nfunction showPlayerStats(id){\n  const player = getApplications().find(app => app.id === id);\n  if(!player) return;\n\n  const stats = getPlayerStats(player);\n  let modal = document.getElementById(\"playerStatsModal\");\n  if(!modal){\n    modal = document.createElement(\"div\");\n    modal.id = \"playerStatsModal\";\n    modal.className = \"player-modal\";\n    document.body.appendChild(modal);\n  }\n\n  modal.innerHTML = `\n    <div class=\"player-modal-card\">\n      <button class=\"player-modal-close\" onclick=\"closePlayerStats()\">\u00d7</button>\n      <img class=\"player-modal-head\" src=\"${mcAvatar(player.username, 128)}\" alt=\"${escapeHtml(player.username)}\">\n      <h2>${escapeHtml(player.username)}</h2>\n      <p class=\"player-modal-team\">Kant: ${escapeHtml(teamLabel(player.team))}</p>\n      <div class=\"stats-modal-grid\">\n        <div><b>${stats.kills}</b><span>Kills</span></div>\n        <div><b>${stats.fish}</b><span>Vissen gevangen</span></div>\n        <div><b>${stats.walked}</b><span>Blocks gelopen</span></div>\n        <div><b>${stats.jumps}</b><span>Jumps</span></div>\n        <div><b>${stats.deaths}</b><span>Deaths</span></div>\n      </div>\n      <p class=\"stats-note\">Stats worden nu handmatig beheerd via het admin panel.</p>\n    </div>\n  `;\n  modal.style.display = \"flex\";\n}\n\nfunction closePlayerStats(){\n  const modal = document.getElementById(\"playerStatsModal\");\n  if(modal) modal.style.display = \"none\";\n}\n\nfunction handleApplicationForm(){\n  const applyForm = document.getElementById(\"applyForm\");\n  if(!applyForm) return;\n\n  applyForm.addEventListener(\"submit\", function(e){\n    e.preventDefault();\n\n    const app = {\n      id: Date.now(),\n      username: document.getElementById(\"mcName\")?.value.trim() || \"\",\n      age: document.getElementById(\"age\")?.value.trim() || \"\",\n      experience: document.getElementById(\"experience\")?.value || \"\",\n      why: document.getElementById(\"why\")?.value.trim() || \"\",\n      contribution: document.getElementById(\"contribution\")?.value.trim() || \"\",\n      hasDiscord: document.getElementById(\"hasDiscord\")?.value || \"\",\n      status: \"pending\",\n      team: \"none\",\n      createdAt: new Date().toLocaleString(\"nl-NL\")\n    };\n\n    if(!app.username || !app.age || !app.experience || !app.why || !app.contribution || !app.hasDiscord){\n      const msg = document.getElementById(\"applyMsg\");\n      if(msg) msg.textContent = \"Vul alle verplichte velden in.\";\n      return;\n    }\n\n    const apps = getApplications();\n    apps.unshift(app);\n    setApplications(apps);\n\n    const msg = document.getElementById(\"applyMsg\");\n    if(msg) msg.textContent = \"Aanmelding verstuurd! Je aanvraag staat nu in het admin panel.\";\n    applyForm.reset();\n    renderAll();\n  });\n}\n\nlet adminApplicationFilter = \"all\";\n\nfunction setAdminApplicationFilter(filter){\n  adminApplicationFilter = filter;\n  document.querySelectorAll(\".filter-btn\").forEach(btn => {\n    btn.classList.toggle(\"active\", btn.dataset.filter === filter);\n  });\n  renderAdmin();\n}\n\nfunction ensureAdminFilters(){\n  const panels = [\"admin-dashboard\", \"admin-aanvragen\"];\n  panels.forEach(panelId => {\n    const panel = document.getElementById(panelId);\n    if(!panel || panel.querySelector(\".admin-filter-bar\")) return;\n\n    const recent = panel.querySelector(\".recent\");\n    if(!recent) return;\n\n    const bar = document.createElement(\"div\");\n    bar.className = \"admin-filter-bar\";\n    bar.innerHTML = `\n      <button class=\"filter-btn active\" data-filter=\"all\" onclick=\"setAdminApplicationFilter('all')\">Alles</button>\n      <button class=\"filter-btn\" data-filter=\"approved\" onclick=\"setAdminApplicationFilter('approved')\">Goedgekeurd</button>\n      <button class=\"filter-btn\" data-filter=\"pending\" onclick=\"setAdminApplicationFilter('pending')\">In behandeling</button>\n      <button class=\"filter-btn\" data-filter=\"denied\" onclick=\"setAdminApplicationFilter('denied')\">Afgewezen</button>\n    `;\n\n    const h2 = recent.querySelector(\"h2\");\n    if(h2) h2.insertAdjacentElement(\"afterend\", bar);\n    else recent.prepend(bar);\n  });\n}\n\nfunction applicationRows(){\n  let apps = getApplications();\n  if(adminApplicationFilter !== \"all\"){\n    apps = apps.filter(app => app.status === adminApplicationFilter);\n  }\n\n  if(apps.length === 0){\n    return `<tr><td colspan=\"6\">Geen aanmeldingen gevonden.</td></tr>`;\n  }\n\n  return apps.map(app => `\n    <tr>\n      <td>${escapeHtml(app.username)}</td>\n      <td>${escapeHtml(app.age)}</td>\n      <td>${escapeHtml(app.experience)}</td>\n      <td>${escapeHtml(app.hasDiscord)}</td>\n      <td>${statusLabel(app.status)}</td>\n      <td>\n        <button class=\"action-btn details\" title=\"Details bekijken\" onclick=\"showApplicationDetails(${app.id})\">\ud83d\udc41\ufe0f</button>\n        <button class=\"action-btn accept\" title=\"Accepteren\" onclick=\"updateStatus(${app.id}, 'approved')\">\u2705</button>\n        <button class=\"action-btn deny\" title=\"Afwijzen\" onclick=\"updateStatus(${app.id}, 'denied')\">\u274c</button>\n        <button class=\"action-btn\" title=\"Verwijderen\" onclick=\"deleteApplication(${app.id})\">\ud83d\uddd1\ufe0f</button>\n      </td>\n    </tr>\n  `).join(\"\");\n}\n\nfunction renderAdmin(){\n  ensureAdminFilters();\n\n  const apps = getApplications();\n\n  const statTotal = document.getElementById(\"statTotal\");\n  const statApproved = document.getElementById(\"statApproved\");\n  const statPending = document.getElementById(\"statPending\");\n  const statDenied = document.getElementById(\"statDenied\");\n\n  if(statTotal) statTotal.textContent = apps.length;\n  if(statApproved) statApproved.textContent = apps.filter(a => a.status === \"approved\").length;\n  if(statPending) statPending.textContent = apps.filter(a => a.status === \"pending\").length;\n  if(statDenied) statDenied.textContent = apps.filter(a => a.status === \"denied\").length;\n\n  const rows = applicationRows();\n  const table1 = document.getElementById(\"applicationsTable\");\n  const table2 = document.getElementById(\"applicationsTable2\");\n  if(table1) table1.innerHTML = rows;\n  if(table2) table2.innerHTML = rows;\n\n  [\"admin-dashboard\",\"admin-aanvragen\"].forEach(id => {\n    const panel = document.getElementById(id);\n    if(panel && !panel.querySelector(\"#applicationDetailsBox\")){\n      const box = document.createElement(\"div\");\n      box.id = \"applicationDetailsBox\";\n      box.className = \"application-details-box\";\n      box.style.display = \"none\";\n      panel.appendChild(box);\n    }\n  });\n}\n\nfunction updateStatus(id, status){\n  const apps = getApplications().map(app => app.id === id ? {...app, status} : app);\n  setApplications(apps);\n  renderAll();\n}\n\nfunction deleteApplication(id){\n  const apps = getApplications().filter(app => app.id !== id);\n  setApplications(apps);\n  closeApplicationDetails();\n  renderAll();\n}\n\nfunction showApplicationDetails(id){\n  const app = getApplications().find(a => a.id === id);\n  const boxes = document.querySelectorAll(\"#applicationDetailsBox\");\n  if(!app || boxes.length === 0) return;\n\n  boxes.forEach(box => {\n    box.innerHTML = `\n      <div class=\"details-card\">\n        <div class=\"details-head\">\n          <h2>${escapeHtml(app.username)}</h2>\n          <button class=\"details-close\" onclick=\"closeApplicationDetails()\">\u00d7</button>\n        </div>\n        <div class=\"details-grid\">\n          <div><b>Minecraft naam</b><span>${escapeHtml(app.username)}</span></div>\n          <div><b>Leeftijd</b><span>${escapeHtml(app.age)}</span></div>\n          <div><b>Ervaring</b><span>${escapeHtml(app.experience)}</span></div>\n          <div><b>Discord username</b><span>${escapeHtml(app.hasDiscord)}</span></div>\n          <div><b>Status</b><span>${app.status === \"approved\" ? \"Goedgekeurd\" : app.status === \"denied\" ? \"Afgewezen\" : \"In behandeling\"}</span></div>\n          <div><b>Kant/team</b><span>${escapeHtml(teamLabel(app.team))}</span></div>\n        </div>\n        <div class=\"details-section\"><b>Waarom wil je meedoen?</b><p>${escapeHtml(app.why)}</p></div>\n        <div class=\"details-section\"><b>Welke roleplay wil je gaan doen?</b><p>${escapeHtml(app.contribution)}</p></div>\n        <div class=\"details-section\"><b>Whitelist command</b><code>/whitelist add ${escapeHtml(app.username)}</code></div>\n        <div class=\"row\">\n          <button class=\"btn small good\" onclick=\"updateStatus(${app.id}, 'approved'); showApplicationDetails(${app.id})\">Accepteren</button>\n          <button class=\"btn small danger\" onclick=\"updateStatus(${app.id}, 'denied'); showApplicationDetails(${app.id})\">Afwijzen</button>\n          <button class=\"btn small\" onclick=\"deleteApplication(${app.id})\">Verwijderen</button>\n        </div>\n      </div>\n    `;\n    box.style.display = \"block\";\n  });\n}\n\nfunction closeApplicationDetails(){\n  document.querySelectorAll(\"#applicationDetailsBox\").forEach(box => {\n    box.style.display = \"none\";\n    box.innerHTML = \"\";\n  });\n}\n\nfunction loginAdmin(){\n  const input = document.getElementById(\"adminCodeInput\");\n  const msg = document.getElementById(\"adminLoginMsg\");\n\n  if(input && input.value === ADMIN_CODE){\n    sessionStorage.setItem(\"wavesmp_admin_logged_in\", \"true\");\n    if(msg) msg.textContent = \"\";\n    updateAdminVisibility();\n    showAdminTab(\"dashboard\");\n  } else if(msg){\n    msg.textContent = \"Verkeerde admin code.\";\n  }\n}\n\nfunction logoutAdmin(){\n  sessionStorage.removeItem(\"wavesmp_admin_logged_in\");\n  updateAdminVisibility();\n}\n\nfunction updateAdminVisibility(){\n  const loginBox = document.getElementById(\"adminLoginBox\");\n  const adminContent = document.getElementById(\"adminContent\");\n  if(!loginBox || !adminContent) return;\n\n  const logged = sessionStorage.getItem(\"wavesmp_admin_logged_in\") === \"true\";\n  loginBox.style.display = logged ? \"none\" : \"block\";\n  adminContent.style.display = logged ? \"grid\" : \"none\";\n}\n\nfunction showAdminTab(tab){\n  const tabs = [\"dashboard\",\"aanvragen\",\"spelers\",\"teams\",\"bans\",\"instellingen\"];\n  tabs.forEach(name => {\n    const panel = document.getElementById(\"admin-\" + name);\n    const btn = document.getElementById(\"tabBtn-\" + name);\n    if(panel){\n      panel.classList.toggle(\"active\", name === tab);\n      panel.style.display = name === tab ? \"block\" : \"none\";\n    }\n    if(btn) btn.classList.toggle(\"active\", name === tab);\n  });\n  renderAll();\n}\n\nlet selectedPublicTeam = null;\nlet selectedAdminTeam = null;\n\nfunction teamPlayersHtml(team){\n  const players = getApplications().filter(app => app.status === \"approved\" && app.team === team);\n  if(players.length === 0){\n    return `<div class=\"empty-state\">Nog geen spelers in ${escapeHtml(team)}.</div>`;\n  }\n  return players.map(player => {\n    const stats = getPlayerStats(player);\n    return `\n      <button class=\"player-card player-card-clickable\" onclick=\"showPlayerStats(${player.id})\">\n        <img class=\"mc-head\" src=\"${mcAvatar(player.username, 96)}\" alt=\"${escapeHtml(player.username)}\">\n        <h3>${escapeHtml(player.username)}</h3>\n        <p><b>Status:</b> ${player.alive === false ? \"<span class=\\\"dead\\\">Dead</span>\" : \"<span class=\\\"alive\\\">Alive</span>\"}</p>\n        <p><b>Kant:</b> ${escapeHtml(team)}</p>\n        <div class=\"mini-stats\">\n          <span>\u2694 ${stats.kills}</span>\n          <span>\ud83d\udc1f ${stats.fish}</span>\n          <span>\ud83d\udc5f ${stats.walked}</span>\n        </div>\n      </button>\n    `;\n  }).join(\"\");\n}\n\nfunction selectPublicTeam(team){\n  selectedPublicTeam = team;\n  document.querySelectorAll(\"#teams .team\").forEach(card => {\n    const name = (card.querySelector(\"b\")?.textContent || \"\").toLowerCase();\n    card.classList.toggle(\"selected-team\", name === team.toLowerCase());\n  });\n\n  let box = document.getElementById(\"publicTeamPlayers\");\n  const teamsPage = document.getElementById(\"teams\");\n  if(!box && teamsPage){\n    box = document.createElement(\"div\");\n    box.id = \"publicTeamPlayers\";\n    box.className = \"team-players-section\";\n    const wrap = teamsPage.querySelector(\".admin-wrap\") || teamsPage;\n    wrap.appendChild(box);\n  }\n\n  if(box){\n    box.innerHTML = `<h2>${escapeHtml(team)} spelers</h2><div class=\"player-grid\">${teamPlayersHtml(team)}</div>`;\n    box.scrollIntoView({behavior:\"smooth\", block:\"nearest\"});\n  }\n}\n\nfunction selectAdminTeam(team){\n  selectedAdminTeam = team;\n  document.querySelectorAll(\"#admin-teams .team\").forEach(card => {\n    const name = (card.querySelector(\"b\")?.textContent || \"\").toLowerCase();\n    card.classList.toggle(\"selected-team\", name === team.toLowerCase());\n  });\n\n  let box = document.getElementById(\"adminTeamPlayers\");\n  const panel = document.getElementById(\"admin-teams\");\n  if(!box && panel){\n    box = document.createElement(\"div\");\n    box.id = \"adminTeamPlayers\";\n    box.className = \"team-players-section\";\n    panel.appendChild(box);\n  }\n\n  if(box){\n    box.innerHTML = `<h2>${escapeHtml(team)} spelers</h2><div class=\"player-grid\">${teamPlayersHtml(team)}</div>`;\n  }\n}\n\nfunction makeTeamCardsClickable(){\n  document.querySelectorAll(\"#teams .team\").forEach(card => {\n    const b = card.querySelector(\"b\");\n    if(!b) return;\n    const team = b.textContent.trim().toLowerCase();\n    const proper = team.charAt(0).toUpperCase() + team.slice(1);\n    card.setAttribute(\"role\",\"button\");\n    card.setAttribute(\"tabindex\",\"0\");\n    card.onclick = () => selectPublicTeam(proper);\n    card.onkeydown = (e) => {\n      if(e.key === \"Enter\" || e.key === \" \") selectPublicTeam(proper);\n    };\n  });\n\n  document.querySelectorAll(\"#admin-teams .team\").forEach(card => {\n    const b = card.querySelector(\"b\");\n    if(!b) return;\n    const team = b.textContent.trim().toLowerCase();\n    const proper = team.charAt(0).toUpperCase() + team.slice(1);\n    card.setAttribute(\"role\",\"button\");\n    card.setAttribute(\"tabindex\",\"0\");\n    card.onclick = () => selectAdminTeam(proper);\n    card.onkeydown = (e) => {\n      if(e.key === \"Enter\" || e.key === \" \") selectAdminTeam(proper);\n    };\n  });\n}\n\nfunction renderTeamOverview(){\n  makeTeamCardsClickable();\n  if(selectedPublicTeam){\n    const box = document.getElementById(\"publicTeamPlayers\");\n    if(box) box.innerHTML = `<h2>${escapeHtml(selectedPublicTeam)} spelers</h2><div class=\"player-grid\">${teamPlayersHtml(selectedPublicTeam)}</div>`;\n  }\n  if(selectedAdminTeam){\n    const box = document.getElementById(\"adminTeamPlayers\");\n    if(box) box.innerHTML = `<h2>${escapeHtml(selectedAdminTeam)} spelers</h2><div class=\"player-grid\">${teamPlayersHtml(selectedAdminTeam)}</div>`;\n  }\n}\n\nfunction renderAll(){\n  renderAdmin();\n  renderPlayers();\n  renderTeamOverview();\n}\n\ndocument.addEventListener(\"DOMContentLoaded\", () => {\n  setActiveNav();\n  handleApplicationForm();\n  updateAdminVisibility();\n  makeTeamCardsClickable();\n  renderAll();\n\n  if(location.pathname.endsWith(\"admin.html\")){\n    showAdminTab(\"dashboard\");\n  }\n});\n\n\n// Alive / Dead player status\nfunction togglePlayerLife(id){\n  const apps = getApplications().map(app => {\n    if(app.id === id){\n      return {\n        ...app,\n        alive: app.alive === false ? true : false\n      };\n    }\n    return app;\n  });\n\n  setApplications(apps);\n  renderAll();\n}\n\n\n// Players page alive/dead filters\nlet playerLifeFilter = \"all\";\n\nfunction setPlayerLifeFilter(filter){\n  playerLifeFilter = filter;\n\n  document.querySelectorAll(\".player-filter-btn\").forEach(btn => {\n    btn.classList.toggle(\"active\", btn.dataset.playerFilter === filter);\n  });\n\n  renderPlayers();\n}\n\nfunction ensurePlayerFilters(){\n  const grid = document.getElementById(\"playersGrid\");\n  if(!grid) return;\n\n  const page = document.getElementById(\"players\");\n  if(!page || page.querySelector(\".player-filter-bar\")) return;\n\n  const bar = document.createElement(\"div\");\n  bar.className = \"player-filter-bar\";\n  bar.innerHTML = `\n    <button class=\"player-filter-btn active\" data-player-filter=\"all\" onclick=\"setPlayerLifeFilter('all')\">Alles</button>\n    <button class=\"player-filter-btn\" data-player-filter=\"alive\" onclick=\"setPlayerLifeFilter('alive')\">Levend</button>\n    <button class=\"player-filter-btn\" data-player-filter=\"dead\" onclick=\"setPlayerLifeFilter('dead')\">Dood</button>\n  `;\n\n  grid.parentNode.insertBefore(bar, grid);\n}\n\n// Override renderPlayers with alive/dead filter support\nconst originalRenderPlayersWithLife = renderPlayers;\nrenderPlayers = function(){\n  ensurePlayerFilters();\n\n  let approved = getApplications().filter(app => app.status === \"approved\");\n\n  if(playerLifeFilter === \"alive\"){\n    approved = approved.filter(app => app.alive !== false);\n  }\n\n  if(playerLifeFilter === \"dead\"){\n    approved = approved.filter(app => app.alive === false);\n  }\n\n  const grid = document.getElementById(\"playersGrid\");\n  if(grid){\n    grid.innerHTML = approved.length ? approved.map(player => {\n      const stats = getPlayerStats(player);\n      return `\n        <button class=\"player-card player-card-clickable\" onclick=\"showPlayerStats(${player.id})\">\n          <img class=\"mc-head\" src=\"${mcAvatar(player.username, 96)}\" alt=\"${escapeHtml(player.username)}\">\n          <h3>${escapeHtml(player.username)}</h3>\n          <p><b>Status:</b> ${player.alive === false ? '<span class=\"dead\">Dead</span>' : '<span class=\"alive\">Alive</span>'}</p>\n          <p><b>Kant:</b> ${escapeHtml(teamLabel(player.team))}</p>\n          <div class=\"mini-stats\">\n            <span>\u2694 ${stats.kills}</span>\n            <span>\ud83d\udc1f ${stats.fish}</span>\n            <span>\ud83d\udc5f ${stats.walked}</span>\n          </div>\n        </button>\n      `;\n    }).join(\"\") : `<div class=\"empty-state\">Geen spelers gevonden met deze filter.</div>`;\n  }\n\n  // Admin grid still needs all approved players\n  const adminGrid = document.getElementById(\"adminPlayersGrid\");\n  if(adminGrid && typeof originalRenderPlayersWithLife === \"function\"){\n    // temporarily render original admin content without touching playersGrid too much:\n    const savedGrid = grid ? grid.innerHTML : null;\n    originalRenderPlayersWithLife();\n    if(grid && savedGrid !== null) grid.innerHTML = savedGrid;\n  }\n\n  if(typeof renderTeamOverview === \"function\") renderTeamOverview();\n\n  document.querySelectorAll(\".player-filter-btn\").forEach(btn => {\n    btn.classList.toggle(\"active\", btn.dataset.playerFilter === playerLifeFilter);\n  });\n};\n\ndocument.addEventListener(\"DOMContentLoaded\", () => {\n  ensurePlayerFilters();\n  renderPlayers();\n});\n";
    document.body.appendChild(script);
  }, []);
  return null;
}
