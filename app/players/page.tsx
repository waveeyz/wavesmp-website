import {supabaseAdmin} from ../../lib/supabase
export default async function Players(){const {data=[]}=await supabaseAdmin.from('applications').select('*').eq('status','accepted').order('created_at',{ascending:false});return <main className="section"><h1 className="title">Players</h1><p className="subtitle">Geaccepteerde spelers verschijnen hier automatisch.</p><div className="cards">{data.map((p:any)=><div className="card" key={p.id}><h3>{p.minecraft_name}</h3><p className="status">Alive</p><p className="muted">Kant: {p.side||'Nog onbekend'}</p><p className="muted">Stats komen later: kills, jumps en distance walked.</p></div>)}</div></main>}
