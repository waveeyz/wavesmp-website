'use client';
import {useState} from 'react';
export default function Aanmelden(){const[done,setDone]=useState(false);async function submit(e:any){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/applications',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(r.ok)setDone(true)}return <main className="section"><h1 className="title">Aanmelden</h1><p className="subtitle">Vul je gegevens in. Een admin bekijkt je aanmelding.</p>{done?<div className="form"><h2>Gelukt!</h2><p className="muted">Je aanmelding is verstuurd.</p></div>:<form className="form" onSubmit={submit}><div className="row"><div><label>Minecraft naam</label><input className="input" name="minecraft_name" required/></div><div><label>Discord naam</label><input className="input" name="discord_name" required/></div></div><label>Leeftijd</label><input className="input" name="age"/><label>Waarom wil je meedoen?</label><textarea name="motivation" rows={5} required/><label>Ervaring met roleplay?</label><textarea name="roleplay_experience" rows={4}/><button className="btn">Versturen</button></form>}</main>}
